class userProfile {
    constructor(id, name, icon_id,parent_id) {
        this.id = id;
        this.name = name;
        this.icon_id = icon_id;
        this.parent_id = parent_id;
    }
}

export default userProfile;