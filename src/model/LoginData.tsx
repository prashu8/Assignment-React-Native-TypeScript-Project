class LoginData {
  constructor(
    _id,
    message,
    role,
    status
  ) {

      this._id = _id;
      this.message = message;
      this.role = role;
      this.status = status;
  }
}

export default LoginData;